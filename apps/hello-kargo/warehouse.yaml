apiVersion: kargo.akuity.io/v1alpha1
kind: Warehouse
metadata:
  name: hello
  namespace: hello
spec:
  freightCreationPolicy: Automatic
  interval: 5m0s
  subscriptions:
    - image:
        repoURL: ghcr.io/jared-paul/hello
        imageSelectionStrategy: SemVer
        semverConstraint: "*"
        strictSemvers: true
        discoveryLimit: 20
